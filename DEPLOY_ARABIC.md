# دليل رفع المشروع على GitHub Pages بالعربية

## 📋 المتطلبات الأساسية

1. حساب GitHub
2. Git مثبت على جهازك
3. Node.js (الإصدار 18 أو أحدث)
4. المشروع موجود في مجلد محلي

---

## 🚀 الخطوات التفصيلية

### الخطوة 1: إنشاء Repository على GitHub

1. اذهب إلى [GitHub.com](https://github.com) وسجل الدخول
2. اضغط على زر **"+"** في أعلى الصفحة واختر **"New repository"**
3. املأ البيانات:
   - **Repository name**: `Cybersecurity-Mentor-Consultant`
   - **Description**: (اختياري) اكتب وصف للمشروع
   - **Visibility**: اختر **Public** (مطلوب لـ GitHub Pages المجاني)
   - **لا** تضع علامة على "Initialize this repository with a README"
4. اضغط **"Create repository"**

---

### الخطوة 2: ربط المشروع المحلي بـ GitHub

افتح Terminal أو PowerShell في مجلد المشروع وقم بتنفيذ الأوامر التالية:

```bash
# تهيئة Git (إذا لم يكن موجوداً)
git init

# إضافة جميع الملفات
git add .

# عمل commit أولي
git commit -m "Initial commit"

# إضافة remote repository (استبدل YOUR_USERNAME باسمك على GitHub)
git remote add origin https://github.com/YOUR_USERNAME/Cybersecurity-Mentor-Consultant.git

# رفع الملفات إلى GitHub
git branch -M main
git push -u origin main
```

**ملاحظة**: استبدل `YOUR_USERNAME` باسم المستخدم الخاص بك على GitHub (مثلاً: `moataz-younes`)

---

### الخطوة 3: التأكد من الإعدادات

المشروع جاهز بالفعل! لكن تأكد من:

#### ✅ ملف `package.json`
- يحتوي على `"homepage"` مع رابط GitHub Pages الصحيح
- يحتوي على scripts: `"predeploy"` و `"deploy"`

#### ✅ ملف `vite.config.ts`
- يحتوي على `base: "/Cybersecurity-Mentor-Consultant/"`

#### ✅ ملف `.nojekyll`
- موجود في `client/public/.nojekyll`

---

### الخطوة 4: تثبيت Dependencies

```bash
npm install
```

هذا الأمر سيثبت جميع المكتبات المطلوبة بما فيها `gh-pages`.

---

### الخطوة 5: بناء المشروع (Build)

```bash
npm run build
```

هذا الأمر سيقوم ببناء المشروع وإنشاء ملفات جاهزة للنشر في مجلد `dist/public`.

---

### الخطوة 6: رفع المشروع على GitHub Pages

```bash
npm run deploy
```

هذا الأمر سيقوم بـ:
1. بناء المشروع تلقائياً (`npm run build`)
2. رفع الملفات إلى branch جديد اسمه `gh-pages`
3. جعل الموقع متاحاً على GitHub Pages

---

### الخطوة 7: تفعيل GitHub Pages في إعدادات GitHub

1. اذهب إلى repository على GitHub
2. اضغط على **"Settings"** (الإعدادات)
3. في القائمة الجانبية، اضغط على **"Pages"**
4. في قسم **"Source"**:
   - اختر **"Deploy from a branch"**
   - Branch: اختر **`gh-pages`**
   - Folder: اختر **`/ (root)`**
5. اضغط **"Save"**

---

### الخطوة 8: الانتظار والتحقق

1. انتظر 2-5 دقائق حتى يتم نشر الموقع
2. اذهب إلى الرابط:
   ```
   https://YOUR_USERNAME.github.io/Cybersecurity-Mentor-Consultant/
   ```
   (استبدل `YOUR_USERNAME` باسمك على GitHub)

---

## 🔄 تحديث الموقع بعد التعديلات

كلما قمت بتعديلات على المشروع:

```bash
# إضافة التغييرات
git add .

# عمل commit
git commit -m "وصف التغييرات"

# رفع التغييرات إلى GitHub
git push origin main

# نشر التحديثات على GitHub Pages
npm run deploy
```

---

## ⚠️ حل المشاكل الشائعة

### المشكلة 1: خطأ 404 عند فتح الصفحة
**الحل**: تأكد من أن `base` في `vite.config.ts` يطابق اسم repository بالضبط

### المشكلة 2: الصور أو الملفات لا تظهر
**الحل**: تأكد من وجود ملف `.nojekyll` في `client/public/`

### المشكلة 3: خطأ في `npm run deploy`
**الحل**: 
```bash
# تأكد من تثبيت gh-pages
npm install --save-dev gh-pages

# حاول مرة أخرى
npm run deploy
```

### المشكلة 4: الموقع لا يحدث بعد النشر
**الحل**: 
- انتظر 5-10 دقائق
- امسح cache المتصفح (Ctrl+Shift+R)
- تأكد من أن branch `gh-pages` تم إنشاؤه بنجاح

---

## 📝 ملاحظات مهمة

1. **اسم Repository مهم**: يجب أن يطابق اسم repository في GitHub مع `base` في `vite.config.ts`
2. **Public Repository**: GitHub Pages المجاني يتطلب repository عام (public)
3. **Branch gh-pages**: يتم إنشاؤه تلقائياً عند تشغيل `npm run deploy`
4. **التحديثات**: قد تستغرق 2-5 دقائق حتى تظهر التحديثات على الموقع

---

## ✅ التحقق من النجاح

بعد النشر، تأكد من:
- [ ] الموقع يفتح بدون أخطاء
- [ ] جميع الصور تظهر بشكل صحيح
- [ ] الروابط تعمل
- [ ] الموقع يعمل على الموبايل
- [ ] النماذج (forms) تعمل

---

## 🎉 تهانينا!

إذا اتبعت هذه الخطوات، سيكون موقعك متاحاً على:
```
https://YOUR_USERNAME.github.io/Cybersecurity-Mentor-Consultant/
```

---

**ملاحظة**: استبدل `YOUR_USERNAME` في جميع الأماكن باسم المستخدم الخاص بك على GitHub.

